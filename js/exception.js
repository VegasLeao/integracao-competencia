// Função para tratar erros
export function tratarErro(mensagem, erro) {
    console.error(`${mensagem} ${erro}`);
    // Adicione aqui qualquer lógica adicional de tratamento de erro, se necessário
}
